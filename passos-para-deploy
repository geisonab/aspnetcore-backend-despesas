dotnet publish -c release

git init

criar arquivo .gitignore
	bin
	obj
	Properties
	.vscode
	appsettings.Development.json

* remover strings de conexão do appsettings e deixar somente no de dev que não sobe para o repositório.
* as strings de connexão de produção serão configuradas direto no azure

git add --all

git commit -m "iniciando repo"

git push -u origin master

criar aplicativo web no azure

configurar o deployment center para o github